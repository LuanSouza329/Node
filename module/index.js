(function dizerOla(){
    console.log("Olá");
})();


/* 
    // Usando o encapsulamento de funções no JavaScript, é possível criar e chamar imediatamente uma função anônima ou nomeada. 
    // Isso é feito utilizando a sintaxe (function() { ... })();, que executa a função assim que ela é definida.

    isso é chamado de Iffe
*/